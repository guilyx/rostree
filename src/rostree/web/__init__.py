"""Static assets for the interactive HTML dependency graph.

Kept as real .css/.js/.html files rather than Python strings so they can be
edited, linted and diffed like the front-end code they are. `core/webview.py`
inlines them into a single self-contained document at generation time.
"""
